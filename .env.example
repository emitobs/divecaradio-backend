# =================================
# DIVECA RADIO - CONFIGURACIÓN
# =================================

# Base de datos MySQL
DB_HOST=localhost
DB_USER=divecaradio_user
DB_PASSWORD=tu_password_seguro
DB_NAME=divecaradio_db

# Servidor
PORT=3002
NODE_ENV=production

# Seguridad (generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
SESSION_SECRET=cambiar_por_una_clave_segura_aleatoria

# CORS - Dominios del frontend permitidos
ALLOWED_ORIGINS=https://divecaradio.com,https://www.divecaradio.com

# Radio Stream
RADIO_STREAM_URL=https://radio-1.waugi.com:8020/radio.mp3

# Logging
LOG_LEVEL=info

